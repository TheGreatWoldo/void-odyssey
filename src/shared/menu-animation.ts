/** Set to false to disable all menu enter/exit animations globally (useful during development). */
export const MENU_ANIMATIONS_ENABLED = true

/** Duration (ms) of each item's enter/exit animation */
export const MENU_ITEM_DURATION_MS = 250

/** Stagger delay (ms) between successive items */
export const MENU_ITEM_STAGGER_MS = 40

/** Extra buffer (ms) added after the last item's animation completes before committing navigation */
export const MENU_EXIT_BUFFER_MS = 50
